import { darkColors, lightColors, ColorTokens } from './colors';
import { spacing } from './spacing';
import { radius } from './radius';
import { typography } from './typography';

export { darkColors, lightColors, ColorTokens, spacing, radius, typography };

export const theme = {
  colors: darkColors, // Default active token palette
  spacing,
  radius,
  typography,
};
